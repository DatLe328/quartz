```cpp
#include<bits/stdc++.h>
using namespace std;
int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	long double u, v;	cin >> u >> v;
	cout << fixed << setprecision(2) << u * u / 4 + v * v / 4;

	return 0;
}

```