Example we have a text `hello` but we want to translate all lower case characters to uppercase characters.
```bash
tr a-z A-Z
```
We can also use to remove character
```bash
tr -d 'l'
```
The text will become `heo`